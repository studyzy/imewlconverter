# 命令行 LLM 参数配置

## 目的
支持在命令行界面配置 LLM 参数，满足自动化脚本和终端用户的需求。

## 需求

### 需求：命令行支持 LLM 参数配置
系统在命令行模式下必须支持通过参数传递 LLM 的 API 地址、API Key 和模型名称。

#### 场景：通过命令行参数生成词频
- **当** 用户在命令行中指定 `--llm-endpoint`, `--llm-key`, `--llm-model` 并请求生成词频
- **那么** 系统必须使用提供的参数通过批量调用的方式请求 LLM API 生成词频
