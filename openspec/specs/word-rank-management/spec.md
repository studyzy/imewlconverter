# 词频管理

## 目的
管理词库中词语的频率评分（Rank），支持多种生成来源。

## 需求

### 需求：选择词频生成来源
系统必须允许用户在可用来源中选择一个用于生成词频。

#### 场景：选择 LLM 作为来源
- **当** 用户在词频生成窗口的下拉列表中选择“LLM 生成”
- **那么** 系统必须在后续操作中使用 LLM 接口获取词频

### 需求：核心生成逻辑
系统应当根据选定的来源为词库中的词语计算或获取词频。
(注：百度和谷歌词频生成器已移除)
